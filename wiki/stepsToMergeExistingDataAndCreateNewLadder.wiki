=export existing data=
now I use squirrel to generate the data script from select sql
==these tables should be backup:==
#player
#message
#authority
#defaultReason
#requestMap
#authority_people

*save the data script*


=create new ladder=
#delete all existing database file, i.e: rm D:\data\TennisLadderOnline\ladderDev.*
#create ladder in BootStrap:
{{{

         // create ladders
         def ls=new LadderService()
         Ladder mixLadder=ls.createLadder("mix doubles",1,2,3,4,10)
         mixLadder.save()
         Ladder singleLadder=ls.createLadder("singles",1,2,3,4,5,6,10)
         singleLadder.save()
         //**///
}}}
#after the first startup successfully, comment out the above code, remember do grails clean before start the server again.

#import the data script from your backup.
